@model IEnumerable<Common.Views.ProductListView>
@if (Model != null)
{
    <div id="products">
        <h2 id="productListPageTitle">
            <span id="title">
                @if (ViewBag.ProductPageTitle != null)
                {
                    @ViewBag.ProductPageTitle
                }
            </span>
        </h2>
        @if (Model.Count() == 0)
        {
            <p>
                There are no products in this category.
            </p>
        }
        else
        {
            @*<div id="sortBy">
                    <h4>Sort By:</h4>
                    <div class="selectbox">
                        <span class="currentItem"><span>Date Listed New to Old</span></span>
                        <div class="list">
                            <ul>
                                <li><a href="#" title="Date Listed New to Old">Date Listed New to Old</a></li>
                                <li><a href="#" title="Date Listed Old to New">Date Listed Old to New</a></li>
                                <li><a href="#" title="Price Low to High">Price Low to High</a></li>
                                <li><a href="#" title="Price High to Low">Price High to Low</a></li>
                            </ul>
                        </div>
                        <select id="ddlSortBy">
                            <option selected="selected" value="1">Date Listed New to Old</option>
                            <option value="2">Date Listed Old to New</option>
                            <option value="3">Price Low to High</option>
                            <option value="4">Price High to Low</option>
                        </select>
                    </div>
                    <div class="cl">
                        &nbsp;
                    </div>
                </div>*@
            @Html.Partial("_ProductListInner")
        }
    </div>
}