<h4>Categories</h4>

<ul>
    @foreach (Common.Views.CategoryView cv in ViewBag.Categories)
    {
        <li>
            @Html.ActionLink(cv.CategoryName, "List", new { categoryId = cv.Id })
            @if (cv.SubCategories != null)
            {
                <ul>
                    @foreach (Common.Views.CategoryView sub in cv.SubCategories)
                    {
                        <li>
                            @Html.ActionLink(sub.CategoryName, "List", new { categoryId = sub.Id })
                        </li>
                    }
                </ul>
            }
        </li>
    }
</ul>